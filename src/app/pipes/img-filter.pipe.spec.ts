import { ImgFilterPipe } from './img-filter.pipe';

describe('ImgFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
